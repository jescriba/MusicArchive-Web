<% @songs.each do |song| %>
  gon.songs.push(JSON.parse("<%= escape_javascript(song.to_json(include: [:artists, :album]).html_safe) %>"))
  $("#song-list").append("<%= escape_javascript(render(:partial => song)) %>");
<% end %>
